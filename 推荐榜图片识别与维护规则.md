# 推荐榜图片识别与维护规则

## 用途

本文件记录推荐榜长图转为程序预设列表时的识别规则。当前已录入 21 个分组：前三个通用培养分组、五种代码主C、`霰弹队`、`暴击队`，以及 11 个版本阵容推荐。

源图署名按图片原文记录为“B站：屑芙蒂”。程序内数据是对图片内容的人工转录，不代表原作者背书；榜单更新时应重新核对原图版本。

## 识别规则

1. 角色身份不以 OCR 得到的中文名作为程序主键。先用头像与官方图鉴匹配，再保存官方稳定 `name_code`。候选头像必须同时包含默认立绘、服装立绘和珍藏品/收藏品解锁的第二立绘，不能只拿默认头像单图比较。
2. 分组标题生成系统预设列表名称；角色按原图从左到右、从上到下排序。
3. 头像上方的短文字（如“7.5秒”）放入 `note`。
4. 右侧、`装备` 上方的橙色评价（如“独一档强度、PVE必练”）也并入 `note`。
5. 右侧固定字段分别录入 `equipment`、`lines`、`skills`、`cube`、`collectible`，不把字段合并成一段不可编辑文本。
6. `红`、`红级` 是榜单用于描述红莲输出量级的专有词。原图出现 `3.5红级`、`1红补红` 等写法时必须完整保留，不得清洗成 `3.5级` 或删掉“红”。
7. 原图无法确认的字不凭空补写；先保留最小可靠内容，并在下次有更清晰来源时修订。
8. 同一角色可出现在多个预设，每个预设保留自己的备注与培养建议。
9. 榜单数据文件为 `src/data/recommendationPresets.js` 和 `src/data/recommendationPresetsExtended.js`。首批三个通用分组放在前者，本次七个主C/队伍分组放在后者；更新数据时不修改图鉴卡片组件。
10. 图鉴和计算器的“默认”顺序必须等于原图从左到右、从上到下的录入顺序。战斗力、等级等仅在用户主动选择后覆盖默认顺序。
11. 版本阵容数据单独放在 `src/data/recommendationLineupPresets.js`。此类分组只转录阵容头像，不根据周围说明臆造装备、词条、技能、魔方或收藏品配置。

## 多立绘头像匹配逻辑

头像匹配不能只取“看起来最像”的单一结果，按下面顺序收敛候选：

1. 从官方角色表建立 `name_code → 默认头像 + 服装头像 + 珍藏品头像` 的一对多关系。多个立绘匹配到同一 `name_code` 时仍视为同一角色。
2. 先读取小卡左侧可见的属性和爆裂阶段图标；如果还能确认企业、职业或武器，也加入约束。
3. 对完整头像区域做匹配，保留若干候选，而不是直接接受相似度第一名。发色、脸部和头饰只能作为视觉特征，不能单独决定身份。
4. 使用属性、爆裂阶段等官方字段过滤候选。视觉相似但属性或阶段冲突的候选必须淘汰。
5. 再用分组标题、角色旁文字和段落说明交叉验证。文字与头像冲突时标记待核对，不能静默采用其中一个。
6. 区分“基础角色的珍藏品第二立绘”和“拥有独立 `name_code` 的限定版本”。例如普通普丽瓦蒂的珍藏品立绘仍是 `5007`，不能因为服装外观误配为 `5106` 的女仆版本。
7. 每次人工纠错后，把最终 `name_code` 顺序加入自动测试，防止后续重新识别时回退到错误结果。

## 版本阵容标题自动识别

以后处理新版长图时，不要求先由用户逐项输入阵容名称。应按以下规则从图片本身建立方案：

1. 先定位“版本阵容推荐”区域，再检测该区域内黑底白字、横向矩形的二级标题；彩色的“代码主C”大标题不属于版本阵容标题。
2. 对每个二级标题单独裁图并 OCR。标题文字先移除空白，再统一全角/半角标点，最后去掉末尾括号及括号内的说明。例如 `镜像容器9阶（伤害溢出不用打水晶鞋了）` 归一化为 `镜像容器9阶`。
3. 一个方案的范围从该标题下方开始，到下一个同级黑底标题之前结束。并排出现的下一个同级标题也会结束当前方案的横向范围，不能把邻组头像混入。
4. 识别区域中的金色小角色卡按从左到右、从上到下读取；灰色 `+` 空位不算角色。
5. 同一标题下如果有两套示例阵容，先按图片顺序合并，再使用官方 `name_code` 去重，保留第一次出现的位置。
6. 小头像先与官方图鉴头像匹配，再用图中相邻说明文字交叉核验。周围文字只能帮助消歧，不能替代头像主键。
7. OCR 标题或头像匹配置信度不足时，必须把该项标成待核对，不能自动生成一个猜测名称。程序内标题归一化由 `normalizeLineupPresetTitle()` 统一执行。

## 更新步骤

1. 保存新版原图，并记录来源与榜单版本日期。
2. 按分组裁图，逐角色匹配头像和 `name_code`。
3. 转录备注及五个培养字段；对 `+`、数字、斜杠和括号逐项复核。
4. 在图鉴中打开对应系统预设，确认角色数量、顺序和卡片信息。
5. 用“已获得”数据验证：系统预设可展示完整推荐名单，但下载和计算只包含账号已获得的交集。
6. 运行项目测试与构建，再更新版本号和升级文档。

## 当前数量

- 1阶段减减冷却角色：8 名
- 灵活搭配辅助：10 名
- 双子辅助：10 名
- 燃烧代码主C：10 名
- 水冷代码主C：10 名
- 风压代码主C：10 名
- 电击代码主C：11 名
- 铁甲代码主C：10 名
- 霰弹队：6 名
- 暴击队：2 名
- 版本推图阵容：8 名
- 克拉肯全自动9阶：7 名
- 克拉肯9阶：5 名
- 过激派9阶：5 名
- 死神9阶：5 名
- 镜像容器9阶：5 名
- 茵迪维利亚9阶：7 名
- 极乐净土塔：5 名
- 泰特拉塔：5 名
- 米西利斯塔：4 名
- 朝圣者/超标准塔：5 名
- 合计：148 个预设条目

## 程序内分组与显示顺序

推荐方案下拉框使用不可选的灰色分组标签。分组和显示顺序统一由 `RECOMMENDATION_PRESET_GROUPS` 管理，图鉴页与计算器不能各自维护另一份顺序：

1. 推图阵容：版本推图阵容。
2. 异常拦截：克拉肯全自动9阶至茵迪维利亚9阶。
3. 辅助角色：1阶段减减冷却角色、灵活搭配辅助、双子辅助。
4. 个突主C：燃烧、水冷、风压、电击、铁甲代码主C。
5. 企业塔：极乐净土塔、泰特拉塔、米西利斯塔、朝圣者/超标准塔。
6. 其他：霰弹队、暴击队。

## 本次识别说明

- 身份以官方图鉴 `name_code` 和官方头像资源交叉核对，界面显示名可继续按简体中文统一。
- 源图里“预计”“推荐”“1红补红”等短备注已合并到 `note`；装备、词条、技能、魔方和收藏品仍保持独立字段。
- 如果以后获得更清晰或更新版本的榜图，应优先修订字段原文，不改变稳定 `name_code`，并重新核对分组顺序。
- 版本阵容推荐的两个示例队伍按首次出现顺序合并；重复头像只保留一次，加号空位和不固定的“最高练度输出”不生成虚构角色。

## 2026-08-17 多立绘复核修正

本次按原图与官方角色表重新核对，修正了以下容易因第二立绘或相近构图误判的角色：

- 1阶段减减冷却角色：小美人鱼、拉毗：小红帽、D：杀手妻子。
- 灵活搭配辅助：普丽瓦蒂、桑迪、海伦：海蓝宝石。
- 水冷代码主C：海伦、普丽瓦蒂、潘托姆、鲁德米拉：冬日之主。
- 风压代码主C：明日香：WILLE、玛娜。
- 电击代码主C：罗珊娜。
- 铁甲代码主C：玛律恰那：海洋进修。
- 霰弹队：舒格、阿尔卡娜：命运伴侣、德雷克。
- 版本阵容：普通普丽瓦蒂、马斯特：浪漫女仆和爱丽丝等不再误配到外观相似的限定版本。
